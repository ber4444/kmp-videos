name: "CodeQL"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '30 1 * * 0'

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'java-kotlin', 'python', 'actions' ]

    # Kotlin 2.4.20-RC is required to resolve Dependabot alert CVE-2026-53914.
    # CodeQL currently supports Kotlin < 2.4.20. continue-on-error ensures CI remains green
    # while CodeQL CLI rolls out support for Kotlin 2.4.20+.
    continue-on-error: ${{ matrix.language == 'java-kotlin' }}

    steps:
    - name: Checkout repository
      uses: actions/checkout@v7

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        tools: latest
        
    - name: Set up JDK 17
      if: matrix.language == 'java-kotlin'
      uses: actions/setup-java@v5
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'gradle'

    - name: Build Java/Kotlin code
      if: matrix.language == 'java-kotlin'
      run: |
        ./gradlew testClasses --no-daemon

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"
