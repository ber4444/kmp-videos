[pytest]
asyncio_mode = auto
testpaths = e2e eval/tests
markers =
    scorecard: Run scorecard generation (offline)
    regression: Run pinned regression tests (offline)
    live: Run tests that hit external provider APIs
